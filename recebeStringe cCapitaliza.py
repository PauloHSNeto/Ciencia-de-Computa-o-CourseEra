def capitalizarString(lista):
    menor=[]
    for x in lista:
        x.strip.capitalize()
    for x in range(len(lista)-1):
        if len(lista[x])<=len(lista(x+1)):
            menor=lista[x]

    menor.

    var = 4 ^ 3
    3.5+5